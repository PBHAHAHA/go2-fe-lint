module.exports = { extends: [ '<%= eslintType === 'index' ? 'go2-fe-eslint-config' :
`require.resolve(go2-fe-eslint-config/${eslintType})` %>', <%_ if (enablePrettier) { _%> 'prettier', <%_ } _%> ],
};
