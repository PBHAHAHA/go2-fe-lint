module.exports = { extends: [require.resolve('<%= eslintType === 'index' ? 'go2-fe-eslint-config' :
`go2-fe-eslint-config/${eslintType}` %>'), <%_ if (enablePrettier) { _%> 'prettier', <%_ } _%> ],
};
